version: "3.8"

services:
  frontend:
    build:
      context: .
    ports:
      - 8000:80
    volumes:
      - ./tmp:/tmp
    networks:
      - mynet
    environment:
      - NAME= "Alpha"
    restart: always

networks:
  mynet:
    driver: bridge
